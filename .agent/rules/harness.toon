@struct Harness version:2 format:toon
@struct
  description: Harness Engineering 框架 Guides/Sensors 分離 + Token 經濟 + 模型路由 + Loop 迴圈紀律
  mode: on-demand
  doc: Docs/04_TOKEN_ECONOMY.md
  supersedes: token_economy.toon, core.agents.toon (Meta-Workflow 段)

@struct LoopProtocol
  description: Loop Engineering 五階段迴圈 每次任務強制走
  stages:
    - Discover: 讀 SQLite 狀態 + 路由表指定 Docs 確認邊界
    - Plan: 拆原子子任務 標目標檔與風險級
    - Execute: Delta-only 實作守鋼鐵戒律
    - Verify: 跑 verification.toon 分層感測器
    - Iterate: 未過 Delta-only 修 最多 3 輪 第 3 輪升級 premium 重試一次 再不過 FAILED 停止
  rules:
    - 連續 3 輪相同輸出觸發上下文重置
    - 超過 50 輪強制終止當前工作流
    - 禁止無限循環自我修正 超上限果斷升級

@struct TokenBudget
  description: 預算參數 全部由 Config.api 載入 禁止散落
  session_budget: 1000000
  warn_watermark_pct: 80
  lock_watermark_pct: 100
  single_call_cap: 200000
  rules:
    - 每次 API 呼叫前本地估算 超過 single_call_cap 先走蒸餾縮減 不送出
    - response usage 寫 SQLite token_ledger
    - 水位 warn 告警 lock 鎖定僅確定性操作可續
    - 每 Phase 檢視每 token 完成任務數 必須單調上升 只看數字不看氛圍

@struct LogicBeforeLlm
  description: 確定性判斷禁用 LLM 清單 能用 Rust 解決一個 token 都不花
  rules:
    - 指令安全檢查 用 sandbox.rs 規則引擎 禁止問模型
    - 成功判定 用 Exit Code + Stderr 禁止信口頭報告
    - token 計數 用本地 estimate_tokens 禁止 API 計數
    - 進度回憶 用 SQLite SELECT 禁止重讀對話
    - 記憶檔分裂 用行級切分計數器
    - 記憶檢索 Stage 0 用 FTS5 倒排索引預過濾

@struct ModelRouting
  description: 非對稱模型路由 路由表進 Config.api.model_routing
  routes:
    - 標籤分類 檔名篩選 語意審查 蒸餾壓縮: flash
    - 代碼生成 複雜推理 終端簽核: primary
    - 同失敗碼三次後升級重試: premium 僅一次
  rules:
    - 語意審查禁用主力模型 一律 flash 級
    - Token 消耗超過 5000 主動路由至精簡模型

@struct PromptCacheAlignment
  description: 提示前綴快取對齊
  rules:
    - request 結構固定 靜態 system 前綴 + 慢變狀態注入 + 快變內容置尾
    - 靜態段位元組級穩定 禁止嵌時間戳與隨機序
    - SQLite 狀態注入段僅在狀態實際變更時改動

@struct LazyActivation
  description: 休眠代理與條件載入 未路由零成本
  rules:
    - 未被路由的代理 不建 task 不呼叫 API 不進 prompt
    - 蒸餾組僅在 token 增量觸及 distill_trigger_delta 時激活
    - MultimodalMediaSpecialist 僅媒體意圖激活
    - 規則檔按 AGENTS.md 路由表條件載入 禁止一次載入全部

@struct DeltaOnly
  description: 增量更新強制 降低 70% Token
  rules:
    - 修正階段一律 unified diff / Edit 精準替換 未變更代碼禁止重複輸出
    - Rust 端 apply_patch 驗證可套用 套不上即 E_SCHEMA REJECT
    - REJECT 回饋只含失敗碼+出錯行+修補指令

@struct SlopAuditor
  description: AI 腔與盲從恭維防腐審查
  banned_words: [delve, testament, underscore, crucial, furthermore, pivotal, moreover, robust, realm, tapestry, embark, intricate]
  rules:
    - 禁止使用 banned_words 任何詞彙
    - 審查輸出強制 [PASS] 或 [REJECT: 檔案:行號 | 原因]
    - 禁止互相恭維或感性感官回覆

@struct PathRouting
  description: 產物路徑分流
  rules:
    - .md 文件進 Docs/
    - .toon 規則進 .agent/rules/
    - 代碼進 src-tauri/src/
    - 禁止 Magic Numbers 必須抽離至 Config 結構體
    - 禁止無意義註解和垃圾代碼

@struct AntiPatterns
  description: 審查時直接 REJECT 的反模式
  rules:
    - 為確認而呼叫 API 確認用 Exit Code 與 SQLite
    - 整檔塞 prompt 只為改一行
    - 讓模型複述任務狀態
    - 語意審查用主力模型
    - 重試時重送相同 prompt 未過 repair table
    - 一次載入全部 .toon 規則

@end
