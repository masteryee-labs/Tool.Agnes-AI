@struct SecurityPolicies version:1 format:toon
@struct
  description: Zero-trust security policies for Agnes AI — key isolation, sandbox alignment, locale calibration
  mode: all

@struct KeyIsolation
  description: API Key must NEVER be hardcoded in .rs files
  rules:
    - API Key 必須寫入 config.local.toml
    - config.local.toml 必須在 .gitignore 中
    - 重開程式自動從 config.local.toml 讀取
    - 掃描所有 .rs 文件禁止出現 sk- 開頭的字串
    - 違反者一票否決

@struct SandboxExitCode
  description: Sandbox exit code alignment — zero false reports
  rules:
    - Exit Code == 0 且 stderr.is_empty() 才判定成功
    - 模型回報成功但 Exit Code != 0 必須判定為虛假回報
    - 自動攔截並將真實 Stderr 砸回主編排官
    - 觸發自愈懲罰循環

@struct DeterministicState
  description: SQLite state machine — no reliance on AI context memory
  rules:
    - 所有原子任務寫入 SQLite（task_status: PENDING/SUCCESS）
    - 每一輪新對話從 SQLite 讀取真實狀態
    - 狀態以硬編碼、不可篡改的 System 上下文形式注入 API Request
    - 禁止依賴長對話 Context 記憶專案進度

@struct DeltaOnlyPatching
  description: Delta-only patching — no redundant full code output
  rules:
    - 中間修正階段禁止重複輸出未變更的整段程式碼
    - 一律使用 Git Diff 增量更新格式
    - 未相關代理人在未被呼叫前保持休眠狀態
    - 降低 70% Token 消耗

@end
